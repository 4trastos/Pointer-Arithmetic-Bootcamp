Ejercicio 10 - Punteros a estructuras constantes

Define una estructura `t_config` con `int mode` y `char name[20]`.  
Crea una función que reciba un puntero constante a `t_config` y muestre sus valores en consola sin modificarlos.  
