Objetivo:
----------
Escribe una función `swap` que reciba dos punteros a int
e intercambie los valores que apuntan.

Prototipo:
----------
void swap(int *a, int *b);

Ejemplo:
--------
int x = 5, y = 10;
swap(&x, &y);
// ahora x == 10 y y == 5
